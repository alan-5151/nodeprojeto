<!DOCTYPE html>
<html>
<head>
	<title> {{h.defaultPageTitle}} {{pageTitle}}</title>
	<meta charset="utf-8" >
	<title>NodeJs</title>
</head>
<body>
	<ul>
		{{#h.menu}}
		<li><a href="{{slug}}">{{name}}</a></li>
		{{/h.menu}}
	</ul>

	<hr />
	{{#flashes}}

	{{#success}}
	<div>{{.}}</div>
	{{/success}}

	{{#error}}
	<div>{{.}}</div>
	{{/error}}

	{{#info}}
	<div>{{.}}</div>
	{{/info}}

	{{/flashes}}